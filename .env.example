NEXT_PUBLIC_API_URL=http://localhost:5000
ENCRYPTION_SECRET_KEY=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000 

# Important: This is an example configuration file for local development.
# Make sure to:
# 1. Copy this file and rename it to .env
# 2. Fill in the appropriate values for each variable
# 3. Use different values for production/deployment environment
# 4. Keep your actual .env file secure and never commit it to version control